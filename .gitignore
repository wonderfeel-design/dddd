# Локальная страница «Где купить» — намеренно не в репозитории (правится отдельно)
gde-kupit.html

# Зависимости terser для сборки head.min.html (ставятся локально через npm install --no-save)
node_modules/
